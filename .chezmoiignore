{{- if ne .chezmoi.hostname "orbiter" -}}
.config/i3
.config/i3blocks
.xinitrc
.Xresources
bin/resize.py
{{- end -}}

# Repo-only contents — tracked in git, NOT deployed to home by chezmoi.
arch-install
arch-install/**

# Omarchy state — generated by `omarchy-theme-set` (theme/, theme.name,
# next-theme/, etc.). Each theme switch rewrites these; chezmoi mustn't
# track or revert them. The user-owned source-of-truth lives in
# .config/omarchy/themes/ (theme overlays) which IS tracked.
.config/omarchy/current
.config/omarchy/current/**
.config/omarchy/themed
.config/omarchy/themed/**

# Walker emergencies state, walker cache, etc.
.local/state/omarchy
.local/state/omarchy/**
