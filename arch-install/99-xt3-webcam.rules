# Start the per-user webcam pipeline when the Fujifilm X-T3 appears. The
# service's ExecCondition handles races and an absent camera without failure.
ACTION=="add", SUBSYSTEM=="usb", ATTR{idVendor}=="04cb", ATTR{idProduct}=="02dd", TAG+="systemd", ENV{SYSTEMD_USER_WANTS}+="xt3-webcam.service"
