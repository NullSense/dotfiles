# Docker owns addressing and routes on these bridge interfaces.
# NetworkManager must not assume or strip their gateway addresses during
# uplink transitions (for example Ethernet -> Android USB tethering).
[keyfile]
unmanaged-devices=interface-name:=docker0;interface-name:br-*
