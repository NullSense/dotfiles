#!/usr/bin/env sh
# omarchy-style helper — kill + respawn waybar (via the keeper).
exec sh -c 'pkill -x waybar 2>/dev/null; sleep 0.3; setsid ~/bin/waybar-keeper >/dev/null 2>&1 < /dev/null & disown; true'
