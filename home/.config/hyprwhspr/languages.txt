# Translation favorites for the omarchy capture menu (Translate region /
# Translate clipboard). One language per line — top entries become the
# walker picker; "Other…" at the bottom takes free-text input that is
# resolved against the daemon's NLLB code table.
#
# Format: name as you'd say it in English. The daemon resolves human names
# ("Lithuanian") and NLLB BCP-47 codes ("lit_Latn") via its internal map.
# Order = display order. No daemon restart needed — read fresh per call.

English
German
Lithuanian
